import styled from"styled-components";export const Overlay=styled.div`position:fixed;inset:0;z-index:190;background:rgba(0,0,0,.45);backdrop-filter:blur(6px);display:flex;`;export const Panel=styled.aside`width:min(100%,${({$width})=>$width});height:100%;margin-left:${({$side})=>$side==='right'?'auto':'0'};margin-right:${({$side})=>$side==='left'?'auto':'0'};background:${({theme})=>theme.colors.surface};border-${({$side})=>$side==='right'?'left':'right'}:1px solid ${({theme})=>theme.colors.border};box-shadow:0 20px 60px ${({theme})=>theme.colors.shadow};display:flex;flex-direction:column;`;export const Header=styled.div`padding:18px 20px;border-bottom:1px solid ${({theme})=>theme.colors.border};display:flex;align-items:center;justify-content:space-between;h2{font-size:${({theme})=>theme.font.size.lg};color:${({theme})=>theme.colors.text};}`;export const Body=styled.div`padding:20px;overflow:auto;flex:1;`;export const Footer=styled.div`padding:14px 20px;border-top:1px solid ${({theme})=>theme.colors.border};display:flex;justify-content:flex-end;gap:8px;`;export const CloseButton=styled.button`width:34px;height:34px;border:0;border-radius:${({theme})=>theme.radius.sm};background:transparent;color:${({theme})=>theme.colors.text};display:grid;place-items:center;cursor:pointer;&:hover{background:${({theme})=>theme.colors.surfaceStrong};}`;
