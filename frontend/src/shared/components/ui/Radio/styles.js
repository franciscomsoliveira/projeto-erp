import styled from"styled-components";export const Wrapper=styled.label`display:inline-flex;align-items:center;gap:8px;color:${({theme})=>theme.colors.text};font-size:${({theme})=>theme.font.size.sm};cursor:${({$disabled})=>$disabled?"not-allowed":"pointer"};opacity:${({$disabled})=>$disabled?.55:1};input{position:absolute;opacity:0;}input:checked + span{border-color:${({theme})=>theme.colors.primary};}input:checked + span::after{opacity:1;}`;export const Circle=styled.span`width:18px;height:18px;border-radius:50%;border:1px solid ${({theme})=>theme.colors.border};background:${({theme})=>theme.colors.input};position:relative;flex-shrink:0;&::after{content:"";position:absolute;inset:4px;border-radius:50%;background:${({theme})=>theme.colors.primary};opacity:0;}`;export const LabelText=styled.span``;
